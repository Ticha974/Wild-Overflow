{% extends 'base.html.twig' %}

{% block title %}All Questions{% endblock %}

{% block stylesheets %}
    {#  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    #}<style>
        .status{
            color: hsl(0,0%,100%);
            padding: 7px 0 5px;
            border: 1px solid transparent;
            background-color: #9d4447;
            margin-bottom: 8px;
            text-align: center;
            border-radius: 50%;
            width: 80px;
        }
        .tag span{
            background-color: #fcabb0;
            border: 1px solid transparent;
            border-radius: 3px;
            padding: 2px;
        }
    </style>
 {{ parent() }}
    <link href="/assets/styles/bootstrap.css" rel="stylesheet">
    <link href="/assets/styles/question.css" rel="stylesheet">
    <link href="/assets/styles/layout.css" rel="stylesheet">
{% endblock %}

{% block javascripts %}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    {% endblock %}


{% block body %}
    <h1 class="header text-center" style="margin-top: 15px;">All Questions</h1>

    {% for question in questions %}
    <div class="container" style=" display: inline-flex; flex-wrap: nowrap; flex-direction: row; align-content: center; align-items: center;">
        <div class="row justify-content-center">
            <div class="col-lg-1">
                <div class="status">
                    <strong>{{ question.answers.count }}</strong>
                    <p><small>Answers</small></p>
                </div>
            </div>

            <div class="container-inner">
                <div class="sticky-container">
                    <div class="sticky-outer">
                        <div class="sticky">
                            <svg width="0" height="0">
                                <defs>
                                    <clipPath id="stickyClip" clipPathUnits="objectBoundingBox">
                                        <path
                                                d="M 0 0 Q 0 0.69, 0.03 0.96 0.03 0.96, 1 0.96 Q 0.96 0.69, 0.96 0 0.96 0, 0 0"
                                                stroke-linejoin="round"
                                                stroke-linecap="square"
                                        />
                                    </clipPath>
                                </defs>
                            </svg>
            <div class="sticky-content">
                <a href="{{ path('question_show', {id: question.id}) }}" style="text-decoration: none; text-shadow: 2px 3px 4px #b75053">
                    {{ question.title }}</a>
                    <div style="font-size: 20px; color: #333333;     text-align: justify; width: 75%;">
                    <p>{{ question.content|u.truncate(100, '...')|raw }}</p>
                    </div>
            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="row-tag">
            <div class="col-lg-7">

                {% for tag in question.tags %}
                <div class="col-lg-1">
                    <p class="tag"><span><small><a href="{{ path('tag_question', {id: tag.id}) }}" class="text-primary">
                                    {{ tag.name }}</a></small></span></p>
                </div>
                {% endfor %}
                <div class="col-lg-3">
                    <p class="text-muted" style="width: 150px"><small>Asked {{ question.createdAt|date("m/d/Y") }}
                    <span class="text-info"> {{ question.user.username }}</span></small></p>
                </div>
            </div>
        </div>
    </div>
                {% else %}
                    <p>No questions yet.</p>
                {% endfor %}
{% endblock %}
